first.className = "text-dark yellow"
first.classList.remove("text-dark");
first.classList.add("text-dark"); //adding back class list.

// first.classList.toggle("yellow") adds the class if it doesn't exist, otherwise removes it.
first.classList.append("yellow");